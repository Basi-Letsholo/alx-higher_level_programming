#include "lists.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * check_cycle - checks if linked list is a cycle
 * @list: linked list
 * Return: 0 if not a cycle, 1 if it's a cycle
 */

int check_cycle(listint_t *list)
{
	/* var to store address of node1 */

	
	while (current != NULL)
	{

	}
}
